%% Architecture — Music Recommender + Reliability / Testing System
%% Starter diagram. This is a placeholder to be filled in as the reliability
%% layer is built (see IMPLEMENTATION_PLAN.md). Nodes marked "(planned)" do not
%% exist in code yet — the current foundation is only load_songs -> score_song
%% -> recommend_songs. Do not treat this as the finished architecture.

flowchart TD
    user["User preferences (user_prefs)"]
    inval["Input validation (planned)"]
    csv["data/songs.csv"]
    load["load_songs()"]
    dsval["Dataset validation (planned)"]
    score["score_song()"]
    rank["recommend_songs()"]
    conf["Confidence scoring (planned)"]
    conflict["Conflict detection (planned)"]
    fallback["Fallback for low confidence (planned)"]
    out["Ranked recommendations + reasons"]
    log["Logging & error handling (planned, cross-cutting)"]

    user --> inval --> rank
    csv --> load --> dsval --> score --> rank
    rank --> conf --> conflict --> fallback --> out
    log -.-> inval
    log -.-> dsval
    log -.-> conf
    log -.-> conflict
    log -.-> fallback
